package com.example.myapplication;

// This is the libraries that we use

import java.io.*;
// In this class we do the example in the next way
public class This_class {
    public static void main(String[] args) {
     int a = 11;
     int b = 22;
     int c = 44;
     System.out.println("Minium value is the next:"+ c);
    }
    // This is the statment in the next model as we can see in the all the lines that we can code

    // The next line is the next part of the main examples as we can see the next subroutine as we can see in the next lines of the code
    public static int MinFunction(int n1, int n2){
        int min;
        if (n1 < n2){
            min = n1;
        }
        else{
            min = n2;
        }
        return min;
    }
};

